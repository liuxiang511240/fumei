<div class="section">
  <div class="top">诗词
    <span style="float: right;font-size:12px;font-weight: normal;">
      <%= link_to '更多...', poetries_path -%>
    </span>
  </div>
  <div class="middle">
    <% @poetries.each do |song| %>
        <%= render :partial => 'layouts/item', :locals => {:item => song} -%>
    <% end %>
  </div>
</div>
<div class="section">
  <div class="top">美文
      <span style="float: right;font-size:12px;font-weight: normal;">
      <%= link_to '更多...', proses_path -%>
    </span>
  </div>
  <div class="middle">
    <% @proses.each do |song| %>
        <%= render :partial => 'layouts/item', :locals => {:item => song} -%>
    <% end %>
  </div>
</div>
<div class="section">
  <div class="top">段子
      <span style="float: right;font-size:12px;font-weight: normal;">
      <%= link_to '更多...', pieces_path -%>
    </span>
  </div>
  <div class="middle">
    <% @pieces.each do |song| %>
        <%= render :partial => 'layouts/item', :locals => {:item => song} -%>
    <% end %>
  </div>
</div>
<div class="section">
  <div class="top">歌曲
      <span style="float: right;font-size:12px;font-weight: normal;">
      <%= link_to '更多...', songs_path -%>
    </span>
  </div>
  <div class="middle">
    <% @songs.each do |song| %>
        <%= render :partial => 'layouts/item', :locals => {:item => song} -%>
    <% end %>
  </div>
</div>